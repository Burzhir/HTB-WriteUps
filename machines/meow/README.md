#Meow completed.

nmaping and after gainin the relavent info which is that its port 23, meaning telnet so we do access by using cmd, telnet <ip>, and we used a common default for telnet specifically, i;e root with no password.
