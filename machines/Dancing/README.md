#Dancing

Nmaping first, after seeing Port - 445(SMB over tcp) or Port - 139(SMB over NetBIOS), connecting through SMB(Secret Message Block) tools, with
cmd smbcient //<ip>/ -N , the -N flag means no password, telling the server to give access if unauthorized access is possible.
